apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-rwo
  labels:
    type: local
spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /tmp/pv-rwo-data
