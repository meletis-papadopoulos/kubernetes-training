apiVersion: v1
kind: Pod
metadata:
  name: event-debug-pod
  namespace: training
spec:
  containers:
    - name: app
      image: nginx:nonexistent-tag
      command:
        - /bin/nonexistent-binary
      ports:
        - containerPort: 80
