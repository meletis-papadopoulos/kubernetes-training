apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
  namespace: training
  labels:
    app: nginx-stateful
spec:
  ports:
    - port: 80
      name: web
  clusterIP: None
  selector:
    app: nginx-stateful
