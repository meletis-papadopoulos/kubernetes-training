apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
  namespace: training
spec:
  containers:
    - name: greedy
      image: busybox:1.36
      command:
        - sleep
        - "3600"
      resources:
        requests:
          cpu: "100"
          memory: 128Mi
