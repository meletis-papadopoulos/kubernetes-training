apiVersion: v1
kind: Pod
metadata:
  name: crashloop-pod
  namespace: training
spec:
  containers:
    - name: failing
      image: busybox:1.36
      command:
        - /bin/sh
        - -c
        - "echo 'Starting...' && exit 1"
  restartPolicy: Always
