apiVersion: v1
kind: Pod
metadata:
  name: config-error-pod
  namespace: training
spec:
  containers:
    - name: app
      image: busybox:1.36
      command:
        - sleep
        - "3600"
      envFrom:
        - configMapRef:
            name: nonexistent-config
