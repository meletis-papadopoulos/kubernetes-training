apiVersion: v1
kind: Pod
metadata:
  name: missing-pullsecret-pod
  namespace: training
spec:
  imagePullSecrets:
    - name: nonexistent-registry-secret
  containers:
    - name: app
      image: ghcr.io/private-org/private-image:1.0
