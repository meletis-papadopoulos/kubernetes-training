apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: training

labels:
  - pairs:
      environment: dev
    includeSelectors: false

patches:
  - path: replica-patch.yaml
