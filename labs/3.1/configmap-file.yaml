apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config-file
  namespace: training
data:
  app-config.properties: |
    database.host=localhost
    database.port=5432
    database.name=myapp
    database.pool.size=10
    cache.enabled=true
    cache.ttl=300
