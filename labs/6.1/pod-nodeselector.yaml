apiVersion: v1
kind: Pod
metadata:
  name: pod-nodeselector
  namespace: training
  labels:
    app: nodeselector-test
spec:
  nodeSelector:
    disktype: ssd
  containers:
    - name: nginx
      image: nginx:1.25
      ports:
        - containerPort: 80
