apiVersion: v1
kind: Pod
metadata:
  name: pod-nodename
  namespace: training
  labels:
    app: nodename-test
spec:
  nodeName: node01
  containers:
    - name: nginx
      image: nginx:1.25
      ports:
        - containerPort: 80
