apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: webapp-tls
  namespace: training
spec:
  secretName: webapp-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  commonName: webapp.training.local
  dnsNames:
    - webapp.training.local
