apiVersion: v1
kind: Pod
metadata:
  name: resourced
  namespace: rq-demo
spec:
  containers:
  - name: web
    image: nginx:1.23.4
    resources:
      requests:
        cpu: 250m
        memory: 64Mi
      limits:
        cpu: 500m
        memory: 128Mi
