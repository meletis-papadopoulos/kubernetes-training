apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-rox
  namespace: reclaim-demo
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 500Mi
  storageClassName: ""
