apiVersion: v1
kind: Pod
metadata:
  name: load-generator
  namespace: scale
spec:
  containers:
  - name: load
    image: busybox:1.36
    command:
    - /bin/sh
    - -c
    - "while true; do wget -q -O- http://web-svc.scale.svc.cluster.local; done"
  restartPolicy: Never
