apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: secure-cert
  namespace: net46
spec:
  secretName: secure-tls
  issuerRef:
    name: selfsigned
    kind: Issuer
  dnsNames:
  - secure.local
  duration: 2160h
  renewBefore: 360h
