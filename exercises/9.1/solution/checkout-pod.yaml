apiVersion: v1
kind: Pod
metadata:
  name: checkout
  namespace: ts91
  labels:
    app: checkout
spec:
  containers:
    - name: app
      image: busybox:1.36
      command:
        - sh
        - -c
        - "echo 'checkout starting'; echo 'settings loaded'; sleep 3600"
  restartPolicy: Always
