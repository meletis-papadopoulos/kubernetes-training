apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: config-demo
data:
  APP_MODE: production
  LOG_LEVEL: info
  app.properties: |
    ui.theme=dark
    ui.locale=en_GB
    feature.beta=true
